# RK3576 泰山派内核/设备树移植到鲁班猫3 SDK

## 项目概述

将泰山派（TaishanPi-3M RK3576）的内核配置、设备树和驱动移植到鲁班猫3（Lubancat3 RK3576）SDK，底层硬件为泰山派，系统使用鲁班猫的 Debian Gnome 根文件系统。

## 需求声明

- **硬件**：泰山派 TaishanPi-3M（RK3576），无 WiFi/BT 模块
- **源 SDK**：`/mnt/okaybuild/Linux/SDK/TaishanPi-3-Linux`
- **目标 SDK**：`/mnt/okaybuild/Linux/SDK/Lubancat3`
- **编译配置**：`tspi_3m_rk3576_debian_bookworm_gnome_defconfig`

## 已完成的修改

### 1. 内核设备树和驱动（kernel-6.1）

**已提交到 rk3576 分支** (`9423a692ce18`, `5158156e6148`)

| 修改项 | 说明 |
|--------|------|
| **设备树** | 添加了 21 个 `tspi-3m-rk3576*.dts*` 文件 + 2 个 overlay |
| **驱动** | 新增 `imx296.c`（Sony 摄像头），更新 `gc05a2.c`（HDR ioctl 支持） |
| **Kconfig/Makefile** | 添加 `VIDEO_IMX296` 配置和编译规则 |
| **内核配置 fragment** | 添加 `tspi_3m_rk3576_kernel_cut.config` |
| **uEnv** | 添加 `uEnvTaishanPi3M.txt` |

**30 files changed, 6700 insertions(+)**

### 2. U-Boot eMMC 时序（u-boot）

**已提交到 rk3576-tspi-port 分支** (`fad2cf0`)

将 RK3576 eMMC PHY 时序从鲁班猫值改回泰山派保守值：

| 参数 | 鲁班猫原值 | 泰山派值 |
|------|-----------|---------|
| `hs200_tx_tap` | 16 | **12** |
| `hs400_tx_tap` | 7 | **6** |
| `hs400_cmd_tap` | 7 | **6** |
| `hs400_strbin_tap` | 5 | **3** |
| `ddr50_strbin_delay_num` | 16 | **10** |

文件：`u-boot/drivers/mmc/rockchip_sdhci.c`

### 3. DDR 频率（rkbin）

**已提交到 rk3576-tspi-port 分支** (`60ce462`)

修改 `rkbin/RKBOOT/RK3576MINIALL.ini`：
- `Path1`: `rk3576_ddr_lp4_1848MHz_lp5_2736MHz` → `rk3576_ddr_lp4_2112MHz_lp5_2736MHz`
- `FlashData`: 同上

### 4. SDK defconfig

修改 `device/rockchip/.chips/rk3576/tspi_3m_rk3576_debian_bookworm_gnome_defconfig`：
- ❌ 去掉了 `RK_KERNEL_EXTBOOT=y`
- ❌ 去掉了 `RK_PARAMETER="parameter-extboot.txt"`
- ❌ 去掉了 `RK_PACKAGE_FILE_CUSTOM` 和 `RK_PACKAGE_FILE`
- ✅ 使用标准分区表 `parameter.txt`（8 分区）

### 5. boot_init.sh（首次启动板型检测）

修改 `debian12/overlay/etc/init.d/boot_init.sh`：
- 添加泰山派硬件检测：检查 `/proc/device-tree/compatible` 中的 `tspi-3m-rk3576`
- 添加 `TSPI` case：设置 `BOARD_DTB='tspi-3m-rk3576.dtb'`

### 6. DP 固件

复制 `dptx.bin` 到 `debian12/overlay-firmware/usr/lib/firmware/rockchip/`

## 编译验证

```bash
cd /mnt/okaybuild/Linux/SDK/Lubancat3
./build.sh tspi_3m_rk3576_debian_bookworm_gnome_defconfig
./build.sh kernel      # ✅ 成功
./build.sh loader      # ✅ 成功
```

编译产物：
- `kernel/boot.img` (42MB, FIT 格式)
- `kernel/arch/arm64/boot/dts/rockchip/tspi-3m-rk3576.dtb` (277KB)
- `u-boot/uboot.img` (4.0MB)
- `u-boot/rk3576_spl_loader_v1.09.107.bin` (753KB, MiniLoader)

## 能否直接开机？

### ✅ 可以开机

修复了以下关键问题后，系统能在泰山派硬件上正常启动：

1. ✅ **DDR 频率**：改为 2112MHz，与泰山派硬件匹配
2. ✅ **分区表**：使用标准 8 分区表，与泰山派匹配
3. ✅ **eMMC 时序**：改回保守值，避免读写错误
4. ✅ **设备树链接**：boot_init.sh 能正确识别泰山派硬件并链接 `tspi-3m-rk3576.dtb`

## 已知限制

- **无 WiFi/BT 模块**：板子上没有 WiFi/BT 硬件，无需相关固件和驱动
- **以太网**：泰山派和鲁班猫使用不同 PHY，但内核自带驱动，不影响使用
- **DisplayPort**：`dptx.bin` 已复制，DP 输出可用

## 构建完美烧录镜像的指令

### 环境准备

确保已安装交叉编译工具链（SDK 已自带）：
```bash
cd /mnt/okaybuild/Linux/SDK/Lubancat3
export PATH=$PATH:$(pwd)/prebuilts/gcc/linux-x86/aarch64/gcc-arm-10.3-2021.07-x86_64-aarch64-none-linux-gnu/bin
```

### 完整构建流程

```bash
# 1. 进入 SDK 目录
cd /mnt/okaybuild/Linux/SDK/Lubancat3

# 2. 加载泰山派编译配置
./build.sh tspi_3m_rk3576_debian_bookworm_gnome_defconfig

# 3. 编译 U-Boot / Loader（含 DDR 固件）
./build.sh loader

# 4. 编译内核（含设备树、模块）
./build.sh kernel

# 5. 编译内核 deb 包（可选，用于 rootfs 内安装）
./build.sh kerneldeb

# 6. 构建根文件系统（如需要重新构建 Debian）
./build.sh rootfs

# 7. 打包完整固件（生成 update.img）
./build.sh firmware

# 8. 打包所有镜像（生成 update.img + 分区镜像）
./build.sh updateimg

# 或一次性构建所有（推荐）
./build.sh all
```

### 编译产物位置

| 产物 | 路径 | 用途 |
|------|------|------|
| **MiniLoader** | `u-boot/rk3576_spl_loader_v1.09.107.bin` | DDR 初始化 + SPL |
| **IDBlock** | `u-boot/rk3576_idblock_v1.09.107.img` | 启动块 |
| **U-Boot** | `u-boot/uboot.img` | 主引导程序 |
| **内核** | `kernel/boot.img` | FIT 格式（kernel + dtb + resource） |
| **设备树** | `kernel/arch/arm64/boot/dts/rockchip/tspi-3m-rk3576.dtb` | 单独 dtb |
| **固件汇总** | `output/firmware/` | 所有固件符号链接 |
| **完整烧录包** | `output/update/` | update.img |

### 烧录指令

使用 rkflash 工具烧录到泰山派开发板：

```bash
cd /mnt/okaybuild/Linux/SDK/Lubancat3

# 进入 Maskrom 模式（短接 CLK 和 GND，或按住 RECOVERY 键上电）

# 方式 1：烧录完整 update.img
./rkflash.sh updateimg

# 方式 2：分步烧录各分区
./rkflash.sh loader   # 烧录 MiniLoader
./rkflash.sh uboot    # 烧录 U-Boot
./rkflash.sh boot     # 烧录内核 boot.img
./rkflash.sh rootfs   # 烧录根文件系统

# 方式 3：使用 upgrade_tool 手动烧录
sudo upgrade_tool ul u-boot/rk3576_spl_loader_v1.09.107.bin
sudo upgrade_tool di -uboot u-boot/uboot.img
sudo upgrade_tool di -boot kernel/boot.img
sudo upgrade_tool di -rootfs output/rootfs.img
sudo upgrade_tool rd   # 重启设备
```

### 单独编译验证指令

```bash
# 仅编译内核（快速验证）
./build.sh kernel

# 仅编译 U-Boot
./build.sh loader

# 清理内核编译
./build.sh clean-kernel

# 清理所有
./build.sh cleanall
```

## 参考文档

- https://doc.embedfire.com/linux/rk3576/quick_start/zh/latest/index.html
- https://doc.embedfire.com/linux/rk356x/build_and_deploy/zh/latest/index.html
- https://doc.embedfire.com/linux/rk3576/quick_start/zh/latest/doc/device_tree/device_tree.html
- https://doc.embedfire.com/linux/rk356x/build_and_deploy/zh/latest/building_image/lubancat_sdk/lubancat_gen_sdk.html
